# TODO

Актуальный бэклог. Реализованное (инкрементальный ingest, `get_tasks` / `get_comments`, `active-board-ids`, фильтр учётных данных, `data/` вне git, systemd-таймер ingest) из списка убрано.

---

## SQL-поиск + векторный поиск

**Статус:** Не начата  
**Колонка:** Идеи  

Подключить полнотекстовый / SQL-поиск (например SQLite FTS или отдельная БД) к текущему Chroma-пайплайну: гибридная выдача или rerank, т.к. на части запросов keyword/BM25 работает лучше, чем только embeddings.

---

## Промпт постановки задачи с предысторией проекта

**Статус:** Не начата  
**Колонка:** Идеи  

Усовершенствовать сценарий создания задачи (`weeek_kb/add/`): при формулировке новой карточки подтягивать контекст по выбранному проекту (недавние задачи, типовые формулировки, колонки) — чтобы LLM не дублировал уже существующее и учитывал предысторию доски.

---

## Регламенты компании в боте

**Статус:** Не начата  
**Колонка:** Идеи  

Подключить базу регламентов (отдельные документы / JSON / индекс), чтобы бот отвечал не только по задачам Weeek, но и на административные вопросы, например:

- постоплата vs только предоплата;
- работа по ЭДО;
- стоимость часа и прочие коммерческие/организационные правила.

Нужен источник правды (файлы в репозитории или Confluence → экспорт), маршрутизация intent (технический вопрос vs регламент) и ответ без смешения с RAG по задачам.

---

<!-- Новые пункты — блоком выше разделителя `---` -->
